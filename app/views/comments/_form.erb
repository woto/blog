<% form_for(@comment) do |f| %>
  <%= f.error_messages %>
  <%= f.hidden_field :post_id %>
  <p>
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
