<span class="tag_level_<%= tag.count %>"><%- tags = Array.[](params[:tags]).flatten.compact -%><%- if(tags).include?(tag.name) -%><%= link_to("-", link_tags(tags.reject{|tag_cmp| tag_cmp == tag.name}, false), :rel => link_tags(tags.reject{|tag_cmp| tag_cmp == tag.name}, true)) %><%- else -%><%- tags << tag.name -%><%= link_to "+", link_tags(tags, false), :rel => link_tags(tags, true) %><%- end -%>&nbsp;<%= link_to h(tag.name), link_tags(tag.name, false), :rel => link_tags(tag.name, true) %></span>,
